```shell
配置姓名:git config <--groble(用户)|--system（系统）> name
配置邮箱:git config <--groble(用户)|--system（系统）> email
初始化仓库:git init [仓库名]
添加文件到cache[stage]:git add [-A] 
提交:git commit -m 'xxx'
git clone [url]

远程同步到本地:git pull [<远程主机名> <远程分支名>:<本地分支名>] 
```



